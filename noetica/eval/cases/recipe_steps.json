{
  "name": "recipe_steps",
  "description": "Decompose an instruction into an ordered list — tests array outputs. Not in the CI subset (output ordering varies by model).",
  "ci": false,
  "match": "schema_only",
  "prompt": "List the steps to make tea as an array of short strings under the key \"steps\".",
  "response_schema": {
    "type": "object",
    "properties": {
      "steps": {
        "type": "array",
        "items": {"type": "string"},
        "minItems": 2
      }
    },
    "required": ["steps"],
    "additionalProperties": false
  },
  "fixture": {"steps": ["Boil water", "Steep the tea bag for 3-5 minutes", "Remove the bag and serve"]}
}
