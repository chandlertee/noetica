# docs/

- [`chat-ui.png`](chat-ui.png) — the chat-UI screenshot used in the README
  (Open WebUI on a local model, answering from a document collection).

The architecture diagram lives inline in the root [README](../README.md) as a
Mermaid block (GitHub renders it natively).
